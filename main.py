# Module Example

# import my_module as mm

# mm.run_module()


# Package Example

from my_package import add, sub, mul, div

fnum= int(input("Enter first number: "))
snum= int(input("Enter second number: "))

add.add_func(fnum, snum)

sub.sub_func(fnum, snum)

mul.mul_func(fnum, snum)

div.div_func(fnum, snum)